# git関連 (alias版)
- regexp-left: ^(?P<body>(grb|g(it)? merge)) +(?P<query>.*)$
  cmd-groups:
    - stmt: git branch --format="%(refname:short)" --all 2>/dev/null
      after: awk '{print $0}'
  fuzzy-finder-cmd: fzf -0 -1 --ansi -q "<query>" --preview='git log --oneline --graph --decorate --color -n20 {}'
  buffer-left: "<body> "
  buffer-right: "[]"
- regexp-left: ^(?P<body>gsw) +(?P<query>.*)$
  cmd-groups:
    - stmt: git branch --format="%(refname:short)" --all 2>/dev/null
      after: awk '{print $0}' | sed -e "s|^origin/||"
  fuzzy-finder-cmd: fzf -0 -1 --ansi -q "<query>" --preview='git log --oneline --graph --decorate --color -n20 {}'
  buffer-left: "<body> "
  buffer-right: "[]"
- regexp-left: ^(?P<body>(g(it)? push [^ ]+|gb -(d|D))) +(?P<query>.*)$
  cmd-groups:
    - stmt: git branch --format="%(refname:short)" 2>/dev/null
      after: awk '{print $0}'
  fuzzy-finder-cmd: fzf -0 -1 --ansi -q "<query>" --preview='git log --oneline --graph --decorate --color -n20 {}'
  buffer-left: "<body> "
  buffer-right: "[]"

# ファイル検索
- regexp-left: ^(?P<body>.*?)(?P<path>~{0,1}([0-9A-Za-z_\-.]*/)+)?(?P<query>[0-9A-Za-z_\-.]*)$
  cmd-groups:
    - tag: 📄file
      stmt: fd -t f -d 5 -H -I -i -E ".git" -p "<query>" -c "always" <path> 2>/dev/null
      after: awk '{print $0}'
    - tag: 📂dir
      stmt: fd -t d -d 5 -H -I -i -E ".git" -p "<query>" -c "always" <path> 2>/dev/null
      after: awk '{print $0 "/"}'
  fuzzy-finder-cmd: fzf -0 -1 --ansi --preview 'if [ -d {} ]; then ls -la {}; else head -20 {}; fi'
  buffer-left: "<body>"
  buffer-right: "[]"
